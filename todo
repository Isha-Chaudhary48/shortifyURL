// need to add copy functionality to the shord id //add a submit button on
analytics // add smt like as user will put full link so i need to change it //
add icon that user is logged in or not //add loading transition //logout
functionality
